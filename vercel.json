{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm build",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "routes": [
    { "src": "/(.*)", "dest": "apps/web/$1" }
  ]
}
