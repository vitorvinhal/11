{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm build && cp -r apps/web/public/* apps/web/.next/",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
