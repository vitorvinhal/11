# 📋 RELATÓRIO FASE 1 — AUTH-GAPS-002 — 2026-09-23

**Status final:** 🟢 CONCLUÍDO COM SUCESSO
**Branch:** `ag4/auth-ui` (base `origin/main` @ 5a0b621)
**Agente:** AGENTE 4 (UNIFY-AUTH & FRONTEND)

## Objetivo Geral

Fechar os 10 gaps de autenticação apontados pela varredura do Brain — rotas em
`apps/web/src/app/api/**` sem `requireUser` — com testes de regressão no mesmo
commit, sem regredir proteções já existentes.

## Metas Esperadas

- [x] `requireUser(req)` na primeira linha de cada handler das 10 rotas (GET incluído) → 401 sem sessão
- [x] Handlers `*/callback` OAuth documentados como públicos por design + rotas de gerenciamento com auth
- [x] Testes de regressão (sem sessão → 401; método alternativo → 401) no mesmo commit
- [x] Clientes afetados ajustados para enviar `Authorization` (STT, OAuth connectors, updates) sem quebrar fluxos
- [x] Relatório pós + commit de segurança

## Roteiro

1. Adicionar `requireUser` (padrão `apps/web/src/lib/auth-unify.ts`) em:
   `stt`, `tts`, `media` (GET/POST/DELETE/PATCH), `system`, `updates`,
   `account`, `connectors/github`, `connectors/google`, `connectors/notion`,
   `connectors/slack`.
2. Em connectors: início de OAuth exige sessão e usa o `userId` autenticado no
   `state` (não o query param); retorno vira JSON `{url}` para o cliente
   redirecionar com `window.location` após fetch autenticado.
3. Callbacks OAuth (`github|google|notion|slack/callback`): comentário
   `// public by design: OAuth callback` (sem auth).
4. Gerenciamento (lista/status/disconnect) já autenticado em
   `connectors/route.ts` (GET/POST/DELETE) — mantido.
5. Clientes: `ChatPanel` (STT com Bearer), `ConnectorsPanel` (fetch auth →
   redirect), `VersionBadge`/`update-client`/`about` (Bearer quando houver
   sessão em `localStorage` `eleven-sb-auth`).
6. `apps/web/e2e/system.spec.ts`: expectativa 401 sem sessão (alinhamento).
7. Teste novo `apps/web/src/app/api/auth-gaps.test.ts` (jest) cobrindo as 10 rotas.
8. Gate `pnpm -r lint && pnpm -r test && pnpm -r build`.

## Arquivos Afetados (previsto)

- `apps/web/src/app/api/{stt,tts,media,system,updates,account}/route.ts`
- `apps/web/src/app/api/connectors/{github,google,notion,slack}/route.ts`
- `apps/web/src/app/api/connectors/{github,google,notion,slack}/callback/route.ts` (comentário)
- `apps/web/src/app/api/auth-gaps.test.ts` (novo)
- `apps/web/src/components/{ChatPanel,ConnectorsPanel,VersionBadge}.tsx`
- `apps/web/src/lib/update-client.ts`
- `apps/web/src/app/about/page.tsx`
- `apps/web/e2e/system.spec.ts`

## 🔄 Diário de Execução em Tempo Real

> ✅ **17:00–17:05** — 10 rotas fechadas com `requireUser` (`stt`, `tts`,
> `media`×4 métodos, `system`, `updates`, `account`, `connectors/{github,
google, notion,slack}`); 4 callbacks OAuth com comentário
> `// public by design: OAuth callback`.
>
> ⚠️ **17:06** — Ponto de decisão: início de OAuth em connectors via
> `window.location.href` não envia `Authorization` → contrato alterado de
> redirect 302 para JSON `{url}` + fetch autenticado no `ConnectorsPanel`;
> `state` do OAuth agora usa o `userId` da **sessão** (não mais query param —
> fecha spoofing de vínculo de conector).
>
> ⚠️ **17:07** — `media` GET/POST agora usam `session.userId` como dono
> (não mais `userId` de query/body — fecha IDOR de listagem/upload).
>
> ⚠️ **17:08** — Clientes sem Bearer ajustados: `ChatPanel` (STT),
> `update-client.fetchUpdates` (lê `eleven-sb-auth` do localStorage),
> `VersionBadge` e `about` passam a usar `fetchUpdates`; e2e
> `system.spec.ts` atualizado para esperar 401.
>
> ✅ **17:10** — Teste novo `auth-gaps.test.ts`: 13 casos (10 rotas +
> métodos alternativos de media) → 172 testes / 17 suites web todos verdes.
> Lint web: 0 errors (17 warnings preexistentes).

## 🏁 Relatório Pós-Alteração (Status Final)

### Tabela rota → status → teste

| #   | Rota                    | Handlers protegidos      | Status                                          | Teste (auth-gaps.test.ts)   |
| --- | ----------------------- | ------------------------ | ----------------------------------------------- | --------------------------- |
| 1   | `api/stt`               | POST                     | ✅ requireUser → 401                            | `POST /api/stt → 401`       |
| 2   | `api/tts`               | POST                     | ✅ requireUser → 401                            | `POST /api/tts → 401`       |
| 3   | `api/media`             | GET, POST, DELETE, PATCH | ✅ requireUser → 401 + dono = sessão            | 4 testes (todos os métodos) |
| 4   | `api/system`            | GET                      | ✅ requireUser → 401                            | `GET /api/system → 401`     |
| 5   | `api/updates`           | GET                      | ✅ requireUser → 401                            | `GET /api/updates → 401`    |
| 6   | `api/account`           | DELETE                   | ✅ requireUser → 401 (substitui getUser inline) | `DELETE /api/account → 401` |
| 7   | `api/connectors/github` | GET (iniciar OAuth)      | ✅ requireUser → 401 + state da sessão          | `GET → 401`                 |
| 8   | `api/connectors/google` | GET (iniciar OAuth)      | ✅ requireUser → 401 + state da sessão          | `GET → 401`                 |
| 9   | `api/connectors/notion` | GET (iniciar OAuth)      | ✅ requireUser → 401 + state da sessão          | `GET → 401`                 |
| 10  | `api/connectors/slack`  | GET (iniciar OAuth)      | ✅ requireUser → 401 + state da sessão          | `GET → 401`                 |

**Exceção documentada (públicas por design):** `connectors/{github,google,
notion,slack}/callback` — recebem redirect do provider OAuth sem sessão;
comentário `// public by design: OAuth callback` adicionado em cada uma.
A rota de gerenciamento (`connectors/route.ts` GET/POST/DELETE —
lista/status/disconnect) **já tinha** `requireUser` — mantida.

### Alterações adicionais (para não quebrar clientes)

- `ConnectorsPanel.handleConnect`: fetch autenticado → `window.location.href = data.url`.
- `ChatPanel` STT: envia `Authorization: Bearer`.
- `update-client.getStoredAccessToken()`: lê session do localStorage `eleven-sb-auth`; `fetchUpdates` envia Bearer.
- `VersionBadge`/`about`: reutilizam `fetchUpdates` (antes fetch cru sem auth).
- `e2e/system.spec.ts`: espera 401 (antes esperava 200 público).

### Ocorrências resolvidas

- Nenhum erro de execução. Lint 0 errors; 172/172 testes web verdes.

<!-- BRAIN_SYNC_START -->

TASK_ID: AUTH-GAPS-002
PHASE: 1/2
STATUS: SUCCESS
<!-- BRAIN_SYNC_END -->
