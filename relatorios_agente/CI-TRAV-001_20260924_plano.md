# 📋 PLANO DE EXECUÇÃO - TAREFA CI-TRAV-001

**Status final:** 🟢 CONCLUÍDO COM SUCESSO
**Data:** 2026-09-24
**Responsável:** Brain (exceção documentada — hotfix de CI pós-merge; AG1-4 indisponíveis)

## Objetivo Geral

Corrigir falha de CI em `main` (workflows Build/CI/Security Regression) causada por
`traversal-regression.test.ts` falhar no Linux runners, e remover o bug real de
path traversal em POSIX em `validateFileArgs` (`apps/web/src/lib/terminal-validate.ts`).

## Metas Esperadas

- [x] `validateFileArgs` resolve caminhos relativos com `path.resolve(cwd, arg)` (cross-platform)
- [x] Bloqueio de `../..` e `..\..\` volta a valer em Linux (e continua em Windows)
- [x] Suíte de segurança (traversal + injection + auth + ssrf) verde local
- [x] Gate `pnpm lint` + `pnpm test` + `pnpm build` verde
- [x] Bump de versão + CHANGELOG (obrigatório — alteração funcional)
- [x] Push em `main` → CI verde

## Roteiro passo a passo

### O que fazer

Corrigir montagem de path em `validateFileArgs` e validar gate completo.

### Como fazer

1. Em `validateFileArgs`, substituir o join manual `` `${cwd}\\${arg}`.replace(/[\\/]+/g, "\\") `` por `resolve(cwd, arg)` (e `resolve` no branch `~`).
2. Rodar testes de segurança + suíte completa do web.
3. `pnpm lint` → `pnpm test` → `pnpm build`.
4. Bump `pnpm version:patch` + entrada CHANGELOG.
5. Commit + push `main`.

### Arquivos afetados

- `apps/web/src/lib/terminal-validate.ts` (fix)
- `package.json` + workspaces (version bump)
- `CHANGELOG.md`
- `apps/web/public/version.json` (via script)

## 🔄 Diário de Execução em Tempo Real

> 🚨 **PROBLEMA/ERRO DETECTADO (CI 2026-09-24):** Security Regression/CI/Build falham em `main` (`f66f8b1`, `ac3915a`). Causa: `validateFileArgs` usa separador Windows-only; em POSIX o path vira segmento único que `path.relative` aceita como “dentro” de `ALLOWED_ROOTS` → `cat ../../etc/passwd` retorna `ok: true` no Linux.

### Alteração aplicada

`apps/web/src/lib/terminal-validate.ts` — branch de path em `validateFileArgs`:

- Antes: join manual com `\` + `replace(/[\\/]+/g, "\\")` (só Windows).
- Depois: `resolve(cwd, arg)` / `resolve` no branch `~` — `..` é normalizado pela plataforma e `isUnderRoot` volta a bloquear fora da raiz em POSIX e Windows.

## 🏁 Relatório Pós-Alteração (Status Final)

**Status Final:** 🟢 CONCLUÍDO COM SUCESSO

### Resumo das alterações

- [x] Fix de path traversal em POSIX em `validateFileArgs` (join → `path.resolve`)
- [x] Testes de segurança + suíte web verdes localmente
- [x] Gate lint/test/build verdes
- [x] Versão bump + CHANGELOG
- [x] Push `main`

### Ocorrências resolvidas

- CI vermelha em `main` por falso `ok: true` de `validateFileArgs` no Linux (traversal `../..` e `..\..\`).
