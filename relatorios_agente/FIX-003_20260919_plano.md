# 📋 PLANO DE EXECUÇÃO - TAREFA FIX-003

**Status Final:** 🟢 CONCLUÍDO COM SUCESSO

## Objetivo Geral

Corrigir as 3 pendências restantes: Skills/Plugins Discover com busca externa, Canvas preview com fallback CDN, e Agent PC visível no desktop.

## Resumo das Alterações

- [x] Agent PC: platform filter `desktop-app` → `desktop-app, desktop-web`
- [x] Agent PC: WebSocket port 4500 → 3001 em 2 arquivos
- [x] Canvas: CDN error indicator + onerror handlers
- [x] Skills Discover API criada (`/api/skills/discover`)
- [x] Plugins Discover API criada (`/api/plugins/discover`)
- [x] SkillsPanel: busca externa com debounce, loading, source badges
- [x] PluginsPanel: busca externa com loading e seção GitHub
- [x] AGENTS.md: regra obrigatória de versionamento + changelog

## Ocorrências

- Nenhuma ocorrência registrada durante a execução.

## Metas Esperadas

- [ ] Skills/Plugins Discover busca externa via GitHub API
- [ ] Canvas preview com tratamento de erro quando CDN falha
- [ ] Agent PC visível na sidebar em desktop-web
- [ ] WebSocket do Agent PC conecta na porta correta (3001)
- [ ] Regra de versionamento + changelog documentada no AGENTS.md

## Roteiro

### Passo 1: Agent PC — Platform Filter + WebSocket Port

**O que fazer:** Alterar `Sidebar.tsx` para incluir `desktop-web` no filter de plataformas do Agent PC, e corrigir porta WebSocket de 4500 para 3001 em `useMobileAgent.ts` e `MobileAgent.tsx`.
**Como fazer:** Editar `platforms: ["desktop-app"]` → `platforms: ["desktop-app", "desktop-web"]` e trocar `localhost:4500` por `localhost:3001`.
**Arquivos afetados:** `apps/web/src/components/Sidebar.tsx`, `apps/web/src/lib/useMobileAgent.ts`, `apps/web/src/components/MobileAgent.tsx`

### Passo 2: Canvas Preview — CDN Error Handling

**O que fazer:** Adicionar `onerror` nos scripts CDN e indicator visual quando CDN falha.
**Como fazer:** Adicionar `onerror="document.getElementById('cdn-error').style.display='block'"` nos scripts e div de erro.
**Arquivos afetados:** `apps/web/src/components/CanvasPanel.tsx`

### Passo 3: Skills Discover API

**O que fazer:** Criar `/api/skills/discover` que busca GitHub API por repos com topics relevantes.
**Como fazer:** Criar route.ts com busca GitHub, normalização de resultados, cache 5min.
**Arquivos afetados:** `apps/web/src/app/api/skills/discover/route.ts` (novo)

### Passo 4: Plugins Discover API

**O que fazer:** Criar `/api/plugins/discover` similar ao skills discover.
**Como fazer:** Criar route.ts com busca GitHub para topics de MCP/plugins.
**Arquivos afetados:** `apps/web/src/app/api/plugins/discover/route.ts` (novo)

### Passo 5: SkillsPanel UI Update

**O que fazer:** Adicionar busca externa no tab Discover com debounce, loading state, e source badges.
**Como fazer:** Adicionar estados `externalResults`, `externalLoading`, função `searchExternal`, merge com catálogo local.
**Arquivos afetados:** `apps/web/src/components/SkillsPanel.tsx`

### Passo 6: PluginsPanel UI Update

**O que fazer:** Adicionar busca externa no tab Descobrir.
**Como fazer:** Similar ao SkillsPanel.
**Arquivos afetados:** `apps/web/src/components/PluginsPanel.tsx`

### Passo 7: AGENTS.md — Regra de Versionamento

**O que fazer:** Adicionar seção sobre obrigatoriedade de bump de versão e changelog.
**Como fazer:** Inserir nova seção após "Arquivos do protocolo".
**Arquivos afetados:** `AGENTS.md`
