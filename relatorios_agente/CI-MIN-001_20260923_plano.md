# 📋 PLANO DE EXECUÇÃO - TAREFA CI-MIN-001

**Status inicial:** 🟡 PLANO CRIADO (PRÉ-ALTERAÇÃO) → **Status final:** 🟢 CONCLUÍDO COM SUCESSO
**Agente:** AGENTE 3 (LLM/CI)
**Branch:** `ag3/ci-minima` (base `origin/main` @ 5a0b621)
**Data:** 2026-09-23
**GO:** CONCEDIDO (escopo mínimo — sem inventar nada além)

---

## Objetivo Geral

Criar a **suíte própria de regressão de segurança** em `apps/web` (hoje inexistente conforme `AGENTS.md`) + **workflow de CI dedicado** que roda apenas esses testes em push/PR para `main`, sem replicar lint/build já cobertos por `build.yml`.

## Metas Esperadas

- [x] (a) Testes de **path traversal**: `../..`, `..\\`, path absoluto fora da raiz bloqueados (`isWithinRoot`/`validateFileArgs`/`validate` de `terminal-validate.ts`).
- [x] (b) Testes de **command injection**: `;`, `&&`, `|`, backtick, `$()` bloqueados (função `validate` + handler `POST /api/terminal/exec`).
- [x] (c) Testes de **auth bypass**: 401 sem sessão em `GET /api/code`, `GET /api/code/read`, `GET|POST /api/terminal/exec`, `GET /api/pc-agent`.
- [x] (d) Testes/verificação de **SSRF**: `/api/settings` e `/api/connectors/test` sem fetch de URL do cliente; detector estático de padrão `fetch` com URL do body em qualquer `route.ts`. Achado de rota pública de URL → REPORTAR (não corrigir).
- [x] Workflow novo `.github/workflows/security-regression.yml` (só install + testes de segurança; sem lint/build).
- [x] Gate final: `pnpm -r lint && pnpm -r test && pnpm -r build` verdes (baseline 415 testes → **458 reais**).
- [x] Commit local (sem push, sem bump, sem deploy).
- [x] Relatório pós com bloco `BRAIN_SYNC`.

## Roteiro passo a passo

1. **Pré (este arquivo):** salvar plano em `relatorios_agente/CI-MIN-001_20260923_plano.md`.
2. **Testes (o que fazer / como fazer):**
   - `apps/web/src/lib/traversal-regression.test.ts` — unitário puro em `terminal-validate.ts`: `isWithinRoot` com `../..`, `..\\..`, absoluto fora, prefixo irmão (`/tmp/app` vs `/tmp/app-evil`); `validateFileArgs` com args fora; `validate` com cwd fora da raiz → `ok:false`.
   - `apps/web/src/lib/injection-regression.test.ts` — `validate()` com `;`, `&&`, `|`, `` ` ``, `$()` → bloqueado; `sanitizeCommand`/`sanitizeArgs` (`code/services/security`); **handler real** `POST /api/terminal/exec` (mock apenas de `auth-unify.requireUser`) com `ls; whoami` etc. → resposta bloqueada (SSE `exit 1`, sem spawn).
   - `apps/web/src/app/api/auth-gaps.test.ts` — handlers reais sem `Authorization`: `GET /api/code` → 401; `GET /api/code/read` → 401; `GET` e `POST /api/terminal/exec` → 401 (mock de `getAuthClient` p/ determinismo; env `NEXT_PUBLIC_SUPABASE_URL` setada antes do require p/ não cair no fallback `dev-user`); `GET /api/pc-agent` → 401 (passa do platform guard via UA vazio → `desktop-web`).
   - `apps/web/src/app/api/ssrf-security.test.ts` — leitura estática: `/api/settings` sem `fetch(`; `/api/connectors/test` só com hosts hardcoded allowlistados (googleapis/slack/github/notion); varredura de todos `apps/web/src/app/api/**/route.ts` contra padrão `fetch(...body|payload|reqBody.(url|endpoint|href)` (regression detector).
3. **Workflow:** `.github/workflows/security-regression.yml` — push/PR → `main`; steps: checkout + node 24 + pnpm 8.15.9 + `pnpm install --frozen-lockfile` + `pnpm --filter @11/web test -- --testPathPattern="(security|auth-gaps|injection|traversal|ssrf|terminal-validate)"`. **NÃO** mexer em `build.yml`/`build-android.yml`/`build-ios.yml`.
4. **Execução e log:** registrar erros em tempo real no rodapé deste arquivo.
5. **Validação:** `pnpm -r lint`, `pnpm -r test` (contagem real vs baseline 415), `pnpm -r build`; checar sintaxe do YAML por leitura (sem disparar workflow).
6. **Pós:** seção final com Status + bloco `BRAIN_SYNC`.

## Arquivos Afetados

| Arquivo                                          | Ação                                                          |
| ------------------------------------------------ | ------------------------------------------------------------- |
| `apps/web/src/lib/traversal-regression.test.ts`  | CRIAR                                                         |
| `apps/web/src/lib/injection-regression.test.ts`  | CRIAR                                                         |
| `apps/web/src/app/api/auth-gaps.test.ts`         | CRIAR                                                         |
| `apps/web/src/app/api/ssrf-security.test.ts`     | CRIAR                                                         |
| `.github/workflows/security-regression.yml`      | CRIAR (novo; não toca nos existentes)                         |
| `relatorios_agente/CI-MIN-001_20260923_plano.md` | CRIAR (este)                                                  |
| Código de produção (`apps/**`, `packages/**`)    | **Só se teste expuser bug pequeno** — caso contrário reportar |

## Restrições (PROIBIÇÕES)

- ❌ `git push` (qualquer branch) — só Brain.
- ❌ `gh workflow run` / disparar workflows — só Brain.
- ❌ Deploy, tags, bump de versão/CHANGELOG (Brain consolida no patch).
- ❌ Modificar `build.yml`, `build-android.yml`, `build-ios.yml` (sem absoluta necessidade).
- ❌ Inventar escopo além dos itens (a)–(d) + workflow.

## Achados de pré-auditoria (para confirmar nos testes)

1. `/api/stt` e `/api/tts` (POST) **não têm `requireUser`** — consomem API keys do servidor sem sessão. **REPORTAR** ao Brain (fora do escopo das 4 rotas citadas em (c)).
2. `apps/desktop/src/router9/safePath.test.ts` existe mas **nunca roda**: `@11/desktop` não tem script `test` e não está nos `projects` do jest raiz. **REPORTAR**.
3. `apps/orca` (`/orca/exec`, retorno 400 real para injection) **sem suíte de testes**. **REPORTAR**.
4. Nenhuma rota pública com fetch de URL arbitrária do cliente encontrada na pré-auditoria (`settings`, `connectors/test`, `chat`, `health/all`, `discover`, `replay`, `keep-alive` = URLs hardcoded/env).
5. Handler `POST /api/terminal/exec` bloqueia injection via SSE `exit 1` (não HTTP 400) — comportamento aceitável (não spawna); observação para o relatório.

---

## 🔄 Diário de Execução em Tempo Real

> 🚨 **PROBLEMA [16:55]** — `pnpm --filter @11/web test -- --testPathPattern=...` → pnpm 8.15.9 rejeitou a opção ("Unknown option: testPathPattern"). **Resolvido:** usar `pnpm --filter @11/web run test -- --testPathPattern=...` (com `run` explícito) — workflow ajustado para essa forma; `--listTests` confirmou os 6 arquivos do pattern.

> 🚨 **PROBLEMA [16:58]** — `ssrf-security.test.ts` falhou compilação TS: comentário de bloco em linha 6 continha `api/**/route.ts` → o `*/` interno fechou o comentário cedo. **Resolvido:** reescrita da linha do comentário.

> 🚨 **PROBLEMA [16:58]** — `auth-gaps.test.ts`: requires dinâmicos com path errado (`../code/route` em vez de `./code/route`). **Resolvido:** paths corrigidos.

> 🚨 **PROBLEMA [16:58]** — `traversal-regression.test.ts`: teste positivo de `validateFileArgs` falhou — a função valida contra `ALLOWED_ROOTS` global (`process.cwd()`), não contra o parâmetro `cwd`. **Resolvido:** caso positivo usa `process.cwd()` como raiz (mesmo padrão do teste existente em `terminal-validate.test.ts`).

> ✅ **[17:02]** Suíte de segurança isolada: **6 suites / 90 testes — TODOS PASS** (4 novos arquivos + 2 existentes no pattern). Workflow `security-regression.yml` criado; sintaxe validada por leitura (yamllint ausente no ambiente; segue estrutura de `build.yml`/`ci.yml`); nenhum workflow existente modificado; nenhum disparo de workflow.

> ✅ **[17:05]** Gate completo (`pnpm -r lint` → `pnpm -r test` → `pnpm -r build`) executado — contagens no Relatório Pós.

> ✅ **[21:33]** Revalidação final nesta sessão: suíte isolada 6 suites/90 tests PASS; apenas novos arquivos = **4 suites / 43 tests**; gate completo re-rodado: **lint 0 errors (warnings pré-existentes) · 458 tests / 35 suites PASS · build OK** (shared→ia→web→desktop→api→cli→mobile). Sintaxe do workflow validada com `js-yaml` → `YAML_OK`. Nenhum workflow existente modificado; nenhum disparo; nenhum push.

---

## 🏁 Relatório Pós-Alteração (Status Final)

**Status Final:** 🟢 **CONCLUÍDO COM SUCESSO**

### Resumo das alterações

- [x] `apps/web/src/lib/traversal-regression.test.ts` — **CRIADO** (13 testes): `isWithinRoot`/`isUnderRoot` com `../..`, `..\\..`, absoluto fora, prefixo irmão; `validateFileArgs` com args fora; `validate` com cwd fora da raiz.
- [x] `apps/web/src/lib/injection-regression.test.ts` — **CRIADO** (18 testes): `validate()`/`hasChainingOperators` com `;`, `&&`, `|`, `||`, `&`, backtick, `$()`; `sanitizeCommand`/`sanitizeArgs`; handler real `POST /api/terminal/exec` recusa antes de spawn (SSE exit 1) + controle positivo.
- [x] `apps/web/src/app/api/auth-gaps.test.ts` — **CRIADO** (7 testes): 401 sem sessão em `GET|POST /api/code`, `GET /api/code/read`, `GET|POST /api/terminal/exec`, `GET /api/pc-agent`; +403 platform guard intacto em pc-agent mobile UA.
- [x] `apps/web/src/app/api/ssrf-security.test.ts` — **CRIADO** (5 testes): `/api/settings` sem `fetch(`; `/api/connectors/test` só hosts hardcoded allowlistados; detector estático em **todas** as `route.ts` de `apps/web/src/app/api` contra `fetch(body|payload|reqBody…)` e `fetch(…searchParams.get…)`.
- [x] `.github/workflows/security-regression.yml` — **CRIADO** (novo): push/PR → `main`; checkout + node 24 + pnpm 8.15.9 + `pnpm install --frozen-lockfile` + `pnpm --filter @11/web run test -- --testPathPattern="(security|auth-gaps|injection|traversal|ssrf|terminal-validate)"`. **Sem** lint/build (já cobertos por `build.yml`). **NÃO** modificou `build.yml`/`build-android.yml`/`build-ios.yml`.
- [x] Nenhum código de produção alterado — nenhum bug de produto exposto pelos testes.

### Gate final (contagens reais)

| Gate                    | Resultado                                                                        |
| ----------------------- | -------------------------------------------------------------------------------- |
| `pnpm -r lint`          | ✅ **0 erros** (warnings pré-existentes: desktop 2, ia 18, web 17)               |
| `pnpm -r test`          | ✅ **458 testes / 35 suites PASS** (baseline **415** → **458** = **+43**)        |
| `pnpm -r build`         | ✅ OK (shared → ia → web → desktop Tauri → api → cli → mobile)                   |
| Suíte segurança isolada | ✅ 6 suites / **90 tests** (4 arquivos novos = **43** + 2 existentes no pattern) |
| Sintaxe workflow        | ✅ `js-yaml` → `YAML_OK` (sem disparo)                                           |

### Bugs encontrados

- **Corrigidos:** 0 em código de produção. Apenas 4 ocorrências de autoria de teste (paths de require, comentário TS, positivo de `validateFileArgs`, flag pnpm `run`) — todas resolvidas e logadas no Diário.
- **REPORTADOS ao Brain (não corrigidos aqui — fora de escopo mínimo):**

  1. **`POST /api/stt` e `POST /api/tts` sem `requireUser`** — consomem `ELEVENLABS_KEY`/`FISH_API_KEY` do servidor sem sessão (confere varredura: ambas `False` para `requireUser`; fetchs são para hosts hardcoded, logo **não** são SSRF, são **auth gap**). Sugerir inclusão no patch do AG4 (`AUTH-GAPS-002`).
  2. **`apps/desktop/src/router9/safePath.test.ts` nunca roda** — `@11/desktop` não tem script `test` e não está nos `projects` do `jest.config.js` (só `apps/web`, `packages/ia`, `packages/shared`); além disso gera 2 warnings de lint (`existsSync`/`realpathSync` não usados).
  3. **`apps/orca` sem suíte de testes** — workspace `apps/*` inclui `orca`, mas só existe `src/server.ts` + `start` (sem `test`); cobertura de `/orca/exec` (injection 400) depende só de smoke manual.
  4. **Observação de contrato:** task pedia "command injection → 400"; o handler real `POST /api/terminal/exec` recusa via **SSE com `exit 1`** (não HTTP 400). Não spawna — comportamento aceitável; teste cobre a recusa.

### Arquivos commitados (somente escopo)

- `apps/web/src/lib/traversal-regression.test.ts`
- `apps/web/src/lib/injection-regression.test.ts`
- `apps/web/src/app/api/auth-gaps.test.ts`
- `apps/web/src/app/api/ssrf-security.test.ts`
- `.github/workflows/security-regression.yml`
- `relatorios_agente/CI-MIN-001_20260923_plano.md`
- `.task_state.md` (atualização de estado)

Não stageados de propósito (ruído de build pré-existente no worktree): `packages/*/dist/**`, `apps/desktop/src-tauri/gen/schemas/**`, `Cargo.toml` (diff 0/EOL).

### Ocorrências resolvidas

- pnpm 8.15.9: `test -- --testPathPattern` → usar `run test -- …` (workflow já ajustado).
- `*/` prematuro em comentário TS do ssrf test.
- Paths de `require` dinâmico no auth-gaps test.
- Positivo de `validateFileArgs` alinhado ao padrão de `terminal-validate.test.ts` (`process.cwd()`).

---

<!-- BRAIN_SYNC_START -->

TASK_ID: CI-MIN-001
BRANCH: ag3/ci-minima
STATUS: SUCCESS
GATE: lint=0 erros (PASS) testes=458/35 suites PASS (baseline 415→458, +43) build=OK
TEST_SUMMARY: 43 novos testes (4 suites: traversal 13, injection 18, auth-gaps 7, ssrf 5) / total 458 · suíte segurança isolada 6 suites/90 tests
REQUIRES_SMOKE_TEST: false
DEPLOY_ACTIONS_TAKEN: NONE
REPORTED_ISSUES: (1) POST /api/stt e /api/tts sem requireUser → sugerir AG4 AUTH-GAPS-002; (2) apps/desktop router9/safePath.test.ts fora do jest root (desktop sem script test); (3) apps/orca sem suíte de testes; (4) terminal/exec recusa injection via SSE exit 1 (não HTTP 400) — aceitável, documentado
<!-- BRAIN_SYNC_END -->
