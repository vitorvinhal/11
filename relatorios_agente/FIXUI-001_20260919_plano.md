# 📋 PLANO DE EXECUÇÃO - TAREFA FIXUI-001

**Status Final:** 🟢 CONCLUÍDO COM SUCESSO

## Resumo das Alterações

- [x] B1: `health/page.tsx` + `admin/page.tsx` iteram `services` (Object.entries) em vez de `checks.map`
- [x] B2: `api/plugins/route.ts` usa coluna `metadata` (select/map/insert/update) + helper `safeParseJson`; install/lista/delete verificados (200, persistência confirmada no Supabase)
- [x] B3: `next.config.js` CSP script-src += `https://cdn.tailwindcss.com https://unpkg.com https://va.vercel-scripts.com`; Canvas renderiza gradiente, sem bloqueio CSP
- [x] B4: `api/pc-agent/route.ts` — `callPCAgent` sem body em GET; GET health reflete `degraded`
- [x] B5/D1: `admin/page.tsx` com Bearer (getAccessToken), parse flat de `/api/version`, shape de metrics real (`riskDistribution`); `e2e/smoke.spec.ts` + `docs/api.md` atualizados
- [x] Regressão: lint 0 erros, jest 385/385, `pnpm build` OK, e2e Playwright 20/20
- [x] `pnpm version:patch` → 2.10.5-alpha + CHANGELOG.md

## Ocorrências registradas

> 🚨 **PROBLEMA DETECTADO [09:20]** — Build falhava: `Property 'ok' does not exist on type 'unknown'` em `admin/page.tsx` (tipagem do Promise.allSettled). Resolvido com casts `as any`.

> 🚨 **PROBLEMA DETECTADO [09:40]** — `/admin` crashava `Cannot read properties of undefined (reading 'safe')`: API de metrics retorna `riskDistribution`, página lia `toolCallsByRisk`. Interface + render corrigidos.

> 🚨 **PROBLEMA DETECTADO [09:50]** — Investiguei `GET /api/plugins` vazio pós-install; confirmado via REST que insert do app persiste (row `data`). GET listou normalmente após. Dados de teste removidos (0 rows restantes).""

## Objetivo Geral

Corrigir 5 bugs reais confirmados no relatório `RELATORIO_TESTE.md` (probe de UI/API em 2026-09-19):

- B1 — `/health` e `/admin` crash (contrato `checks[]` vs `services{}`)
- B2 — `/api/plugins` 500 (`column plugins.config does not exist`, migration usa `metadata`)
- B3 — CSP bloqueia Canvas (Tailwind/React/Babel CDN) e Vercel Analytics
- B4 — `/api/pc-agent` GET com body → `Request with GET/HEAD method cannot have body`
- B5/D1 — `/admin` sem Bearer nos fetches (401) + contrato flat de `/api/version` (e2e+docs desatualizados)

## Metas Esperadas

- [ ] B1: `/health` e `/admin` renderizam sem error boundary
- [ ] B2: GET/POST/DELETE `/api/plugins` → 200 com usuário real
- [ ] B3: Canvas preview estilizado (gradiente) + Analytics/SpeedInsights carregam; console sem "blocked by CSP"
- [ ] B4: `GET /api/pc-agent` sem exceção, status reflete degraded
- [ ] B5/D1: `/admin` mostra dados reais (version/metrics/plugins/skills); e2e smoke 20/20; docs/api.md coerente
- [ ] Regressão: lint 0 erros, teste 385/385, build root ok
- [ ] `pnpm version:patch` + CHANGELOG.md + commits

## Roteiro

### FASE 1: B2 — Plugins (sem tocar schema remoto, usar coluna `metadata`)

**O que fazer:** trocar uso de `config` por `metadata` em `apps/web/src/app/api/plugins/route.ts` (select/map/insert/update).
**Como:** select `metadata`; ler `p.metadata` (tratar string→obj); insert/update enviando objeto (PostgREST serializa JSONB).
**Arquivos:** `apps/web/src/app/api/plugins/route.ts`

### FASE 2: B1 — Health + Admin (services em vez de checks)

**O que fazer:** em `health/page.tsx` e `admin/page.tsx` iterar `services` (Object.entries) em vez de `checks.map`. Corrigir latência opcional.
**Arquivos:** `apps/web/src/app/health/page.tsx`, `apps/web/src/app/admin/page.tsx`

### FASE 3: B5 + D1 — Admin autenticado + contrato de versão

**O que fazer:** `admin/page.tsx` usar `getAccessToken()` + header Bearer; parse flat de `/api/version`; `plugins`/`skills` fora de `data`. Atualizar `e2e/smoke.spec.ts` e `docs/api.md`.
**Arquivos:** `apps/web/src/app/admin/page.tsx`, `apps/web/e2e/smoke.spec.ts`, `docs/api.md`

### FASE 4: B4 — PC Agent GET health

**O que fazer:** `callPCAgent` sem body em GET; `GET /api/pc-agent` status `degraded` quando um serviço falhar.
**Arquivos:** `apps/web/src/app/api/pc-agent/route.ts`

### FASE 5: B3 — CSP allowlist CDNs

**O que fazer:** `next.config.js` script-src += `https://cdn.tailwindcss.com https://unpkg.com https://va.vercel-scripts.com`.
**Arquivos:** `apps/web/next.config.js`

### FASE 6: Regressão

**O que fazer:** `pnpm lint` → `pnpm test` → `pnpm build` (root) → e2e Playwright → probe de UI (login, abas, rotas).
**Arquivos:** — (verificação)

### FASE 7: Versionamento + entrega

**O que fazer:** `pnpm version:patch`, CHANGELOG.md, commits por fix, `.task_state.md`, relatório pós-alteração.
**Arquivos:** `package.json`, `apps/web/public/version.json`, `apps/*/package.json`, `packages/*/package.json`, `CHANGELOG.md`, `.task_state.md`, `relatorios_agente/FIXUI-001_20260919_plano.md`
