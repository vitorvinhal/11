# Task State — Crash Recovery

## Objetivo Global

Refatoração completa da "11" (Modo Astra/UI ChatGPT, OpenRouter, Eleven Coder, salvaguardas, dry-run, resiliência offline, CI/CD) + túnel para Eleven.

## Concluído

- Túnel cloudflared novo ativo: https://other-expansion-robbie-fashion.trycloudflare.com → localhost:20128
- Segurança (já existia da FASE 21): auth /router9, safePath realpath, auth PATCH/DELETE sessões, JWT_SECRET fail-fast, terminal/exec bloqueio prod
- CostBreaker com contabilização real (uso de tokens reais + persistência model_usage)
- CLI templates criados (packages/cli/templates/{web,api,mobile}/main.hbs)
- apps/mobile/tsconfig.json: noEmit:false
- .env.example: OPENROUTER_API_KEY/MODEL/ALLOWED_MODELS + PAID_MODEL_DAILY_BUDGET
- apps/web dev: porta fixa 3000
- skills_projects.sql: vector(1536)→vector(768) (unificou com init.sql/embedding-dims)
- Migration nova: 20240927_audit_vault_memory.sql (action_risk_rules, tenant_vault, memories origin/scope/status/superseded_by, memory_events)
- OpenRouter adapter (packages/ia/src/router/adapters/openrouter.ts) + registry + AUTO_ORDER
- ModelGateway: perfis de roteamento (cost/latency/quality) + método compare() + export costBreaker
- /api/compare (side-by-side até 3 modelos) + /api/reverter + /api/memoria + /api/finops
- Memória auditável: supersedeMemory, logMemoryEvent, getMemoryAudit (packages/ia/src/agent/memory.ts)
- PWA offline: public/sw.js (cache shell + IndexedDB queue + Background Sync), ServiceWorkerRegister, lib/offline-queue, banner GATEWAY_UNAVAILABLE no ChatPanel
- Tema Astra: #05050A, glassmorph utilities, favicons minimalistas mantidos
- UI: AppShell extraído, rotas /coder /canvas /neural /memoria, CanvasPanel, MemoriaPanel, FinOpsPanel, Sidebar agrupamento por data + nav nova
- ChatPanel: perfil de roteamento, botão Comparar + modal, fila offline
- Typecheck web OK (corrigidos 3 erros pré-existentes em testes: cache.test, event-emitter.test, job-queue.test)

## Em Andamento

- Bateria de testes/typecheck/build de todos os pacotes (api, desktop, mobile)
- Controlar lint do web (novos arquivos)

## Próximo Passo

- Rodar pnpm test (jest 336+), typecheck api/desktop/mobile, lint web
- SemVer bump, TLOG.md + changelog, commit semântico + tag + push + deploy
