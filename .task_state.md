# Task State — v2.12.0-alpha (Atualizações + Pendências)

## Objetivo Global

Sistema de upgrade do app (mobile e desktop): notificar a cada versão nova, checar no perfil e baixar a nova versão. Concluir pendências (migração + cap sync).

## Concluído

- `/api/updates` (versão + changelog + links de download por plataforma)
- `update-client.ts`: versionCode como referência, acknowledge/notify persistido, download por plataforma
- Notificação a cada versão nova: nativa via DeviceBridge/LocalNotifications (mobile), PC Agent/Notification API (desktop), banner in-app + badge no perfil
- ProfileDialog: aba "Atualizações" (buscar, changelog, baixar) + prop initialTab/updateBadge; AppShell: checagem no boot
- Mobile: `cap sync` OK — 8 plugins registrados no nativo (android)
- Bump 2.12.0-alpha + CHANGELOG; lint/build web OK; ia testes 257; mobile tsc OK

## Em Andamento

- Nenhum (commit pendente)

## Próximo Passo

- Commit + push; aplicar migração `20260919_devices_gateway.sql` (comando abaixo); build APK
  `supabase link --project-ref uacqekmejviqddrbnfhg && supabase db push`
