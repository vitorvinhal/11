# Task State — v2.10.17-alpha (Eleven Code: reboot do Orca no deploy)

## Objetivo Global

Substituir o sistema antigo "Eleven Coder" (Monaco/XTerm) pela UI completa do **Orca** (stablyai/orca, MIT) embutida como bundle único na aba **Eleven Code** (desktop-app, desktop-web, mobile-app), sem iframe e sem servidor próprio — com o bundle **regenerado a cada deploy** (rebuild no buildCommand do Vercel).

## Concluído

- Bundle Orca em `apps/web/public/orca-inline/` (entry ESM `orca.js` exporta `mountOrca`)
- `ElevenOrca.tsx`: carrega CSS + bundle via `import(/* webpackIgnore */ url)` e monta em `<div>`; fallback de erro
- Código legacy removido: `CodePanel`, `CodeWorkspace`, `ElevenCoder`, `TerminalPane`, `TerminalPanel`, `monaco-*`, rota `/coder`, `build-monaco-workers.mjs`
- Deps removidas do web: `@monaco-editor/react`, `monaco-editor`, `@xterm/*`
- `Sidebar.tsx`: aba "coder" removida; "Eleven Code" mantida nas 3 plataformas
- **REBUILD NO DEPLOY**: novo `scripts/build-orca-bundle.mjs` clona `stablyai/orca` (pinned `e476193b`) em `vendor/.orca-src` (gitignored), injeta entry `eleven-mount.tsx` + `vite.eleven.config.ts`, builda ESM único e publica em `public/orca-inline`; vercel.json roda o script antes do `next build` com fallback resiliente (status 0 se falhar)
- Teste local do script: bundle regenerado (1504 arquivos, `mountOrca` presente, orca.css estável)
- Validações: lint 0 erros · 130 testes web pass · build web OK · shared/ia OK
- Bump `pnpm version:patch` → **2.10.17-alpha**; CHANGELOG atualizado

## Em Andamento

- (nenhum — conclusão)

## Próximo Passo

- Commit + push → deploy Vercel usa rebuild do bundle → validar aba Eleven Code (WebConnect/App) em produção
