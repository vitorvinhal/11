# Task State — v2.10.0-alpha (Deploy Complete)

## Objetivo Global
Fix UI bugs, correct API contracts, implement platform-specific features (desktop/mobile/web), set up production infrastructure (ngrok tunnel for 9Router + Ollama).

## Concluído
- Full repo inventory, menu mapping (15 nav items), API endpoint testing (30/33 working)
- Fixed ThemeToggle overlap in layout.tsx
- Fixed Skills API (create action handler) and Plugins API (POST contract + DELETE handler)
- Platform detection library: `src/lib/platform.ts` (4 platform types, `usePlatform()` hook)
- Platform guards: `src/lib/platform-guard.ts` (desktopOnly, mobileOnly, appOnly, webOnly)
- Sidebar updated: hides "Eleven Coder" + "Agente PC" on web, shows "Agente Mobile" on mobile
- New API routes: `/api/pc-agent` (desktop), `/api/mobile-agent` (mobile)
- New component: `MobileDevicePanel.tsx`
- Desktop unified server: `apps/desktop/src/server.ts`
- ngrok configured with stable URL: `https://synergy-angelfish-robotics.ngrok-free.dev`
- Vercel env vars updated (Production, Preview, Development)
- Deploy scripts: `scripts/deploy-vps.sh` + `scripts/deploy-vps.ps1`
- RELATORIO_MENUS.md audit report
- Git: committed + pushed to GitHub (commits 163ce52, 5ebe44b)
- Vercel: Production deploy READY (https://11-r2kctyq81-vitorvinhal90-8297s-projects.vercel.app)

## Em Andamento
- VPS deployment (blocked on user creating Oracle Cloud Free Tier account)

## Próximo Passo
1. User creates Oracle Cloud account → provisions VM.Standard.A1.Flex (4 CPU, 24GB RAM)
2. SSH into VPS → run `scripts/deploy-vps.sh`
3. Update Vercel env vars with stable VPS tunnel URL
4. Verify chat works end-to-end in production
