# Task State — v2.10.14-alpha (Eleven Code no padrão do Orca)

## Objetivo Global

Transformar a aba **Eleven Code** usando o código pronto do **Orca** (stablyai/orca, MIT), com relatório + testes completos do projeto.

## Concluído

- Clone completo do `stablyai/orca` em `C:\Users\Administrator\Documents\orca`
- Testes da base (v2.10.4): lint 0 erros · 385/385 testes · build OK
- **R1 Monaco**: `monaco-setup.ts`, `monaco-languages/`, `monaco-find-options.ts` portados do Orca; CodePanel → Monaco Editor
- **R2 Terminal+busca**: `TerminalPane.tsx`; split editor+terminal no CodePanel; busca Ctrl+F no ElevenCoder
- **R3 Fix deploy**: `vercel.json` sem `rootDirectory` — deploys automáticos voltaram a dar Ready
- **R4 UX**: CodeWorkspace abre no editor Monkey por padrão; aba renomeada "Eleven Code"
- **R5/R6 Fix build Vercel**: workers output fora do webpack — `scripts/build-monaco-workers.mjs` (esbuild → `public/vs/`) + `clear-next-cache.mjs`; `monaco-setup.ts` aponta `/vs/*`
- Bumps até **2.10.14-alpha**; CHANGELOG atualizado

## Em Andamento

- Push da R6 (build Vercel determinístico)

## Próximo Passo

- Push → aguardar deploy automático **Ready** → validar produção com hard refresh
