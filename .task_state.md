# Task State — v2.16.3-alpha (integração multi-agente em andamento)

## Objetivo Global

INTEGR-AGENTS-001: integrar branches dos agentes (whiting, fix/orca-browser, main) em `integration/agents-20260923` + padronizar segurança (`requireUser`, SSRF allowlist, path.relative).

## Concluído

### TASK-UNIFY-AUTH-001 (base candlefish)

- `apps/web/src/lib/auth-unify.ts` criado (`requireUser(req)` → `{userId,email}`)
- `terminal/exec/route.ts` + `code/route.ts` + `code/services/security.ts` migrados
- `console.log`→`console.warn` em `packages/ia`; linha `coder` removida do CLAUDE.md
- lint 0 erros · 415 testes pass · builds shared/ia/web ok

### TASK-OLLAMA-LLAMA (branch whiting — mergeado)

- Fix default Ollama `llama3.2:3b`, fallback de modelo inexistente via `/api/tags`
- `OLLAMA_POPULAR` ampliado com Llama
- Merge `vitorvinhal/whiting` → `integration/agents-20260923` (conflitos resolvidos: CHANGELOG combinado, package.jsons 2.16.3-alpha)

### Fix Eleven Code (FIX-ELEVENORCA-001) — CONCLUÍDO (fix/orca-browser — mergeando)

- [x] Shim `process`/`Buffer` em `ElevenOrca.tsx`
- [x] CSS `/orca-inline/assets/orca.css`
- [x] `build-orca-bundle.mjs` define process.*/Buffer
- [x] PR #3 merged → `466b30b`

### Pareamento Orca (PAIR-ORCA-001) — EM ANDAMENTO

- [x] Comando `orca serve --pairing-address <host> --json`
- [ ] Executar `orca serve` local e capturar Pairing URL
- [ ] Colar URL na UI e validar conexão

## Em Andamento

- Merge `fix/orca-browser` (conflitos `.task_state.md`/`CHANGELOG.md` resolvendo)
- Pendente: merge de `main` (Agente 1 — Orca) por último; converter `verifyToken`→`requireUser` em `code/read/route.ts`

## Pendências restantes

1. **iOS IPA**: artifact do Actions → Sideloadly (7 dias revalidar).
2. **Validação em aparelho real**: device Android/PC end-to-end.
3. **PAIR-ORCA-001**: subir `orca serve`, capturar Pairing URL.
4. Commit docs `ef659e5` (pós FIX-ELEVENORCA) só em `fix/orca-browser`.

## Próximo Passo

- Completar merge `fix/orca-browser` → merge `main` → pass de segurança → gates (`pnpm -r lint/build/test`).
