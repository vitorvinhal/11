# Task State — v2.12.2-alpha (DEPLOY EM PRODUÇÃO)

## Objetivo Global

Agente de Dispositivo (PC/Mobile) + sistema de atualização + pendências + deploy em produção.

## Concluído

- Agente PC/Mobile 100% funcional (21 tools device.*, pareamento, jobs, approval) — entregue
- Sistema de upgrade: notificação a cada versão, aba Atualizações no perfil, downloads
- Pendências: APK release assinado, re-trigger pós-aprovação, fixes desktop (dist-server, CORS, JWT default)
- **Deploy em produção**: mergeado via PR #2 (`85097cd` na main) + fix buildCommand cwd-independente
- **Produção LIVE em `https://11-app-sage.vercel.app` → v2.12.2-alpha (code 20)**
- `/api/updates` e `/downloads/*` (APK release 6.4MB, setup.exe, MSI) respondendo 200 em produção

## Em Andamento

- Nenhum

## Única pendência bloqueada por ambiente

1. **Migração Supabase** (`20260919_devices_gateway.sql`): sem credenciais (token/db password) neste ambiente.
   Comando (rodar onde houver acesso):
   ```bash
   supabase link --project-ref uacqekmejviqddrbnfhg
   supabase db push
   ```
2. **iOS**: requer build em macOS (este ambiente é Windows).
