# Task State — v2.16.2-alpha (fix Eleven Code em produção — PR #3 merged)

## Objetivo Global

Agente de Dispositivo (PC/Mobile) + sistema de atualização + builds atuais + deploy.

## Fix Eleven Code (FIX-ELEVENORCA-001) — CONCLUÍDO

- [x] Sincronizado com origin/main (21 commits, era 2.14.0-alpha)
- [x] Shim `process`/`Buffer` em `ElevenOrca.tsx` commitado
- [x] CSS corrigido: `/orca-inline/assets/orca.css` (raiz dava 404)
- [x] `build-orca-bundle.mjs`: define process.*/Buffer
- [x] PR #3 merged → `466b30b`, CI verde, deploy Vercel no workflow
- [x] **Validado**: tela Eleven Code carrega em produção (screenshot do usuário)

## Pareamento Orca (PAIR-ORCA-001) — EM ANDAMENTO

- [x] Comando gerador identificado: `orca serve --pairing-address <host> --json`
- [x] Fonte confirmada: `vendor/.orca-src/docs/reference/headless-linux-server.md` (linhas 108–167)
- [x] Orca instalado em `C:\Users\Administrator\AppData\Local\Programs\orca\resources\bin\orca.exe`
- [ ] Executar `orca serve` local e capturar Pairing URL
- [ ] Colar URL na UI de produção (Eleven Code → Connect to Orca) e validar conexão

## Concluído (pipeline completo)

- Agente PC/Mobile (21 tools device.*, pareamento, jobs, approval) — entregue
- Sistema de upgrade (notificação + perfil Atualizações + downloads) — entregue
- Migração Supabase `20260919_devices_gateway.sql` — **APLICADA** (CLI, sem senha via access token)
- Builds atuais **2.12.2**: desktop (exe 1.5MB / msi 2.4MB) + APK release 6.4MB — em `/downloads`
- **Produção live** (`candlefish.vercel.app` / `11-app-sage.vercel.app`): version 2.12.2 (code 20), downloads batendo byte-a-byte
- **CI/GitHub Actions na main** (`e32e070`): workflows Build (lint/test/build), Android (APK release), iOS (macOS na nuvem, IPA unsigned)
- rootDirectory do projeto Vercel = apps/web (deploys GH e CLI OK)
- Guia instalação pessoal: `apps/mobile/PERSONAL_INSTALL.md` (Android direto; iOS via Sideloadly + Apple ID gratuita)

## Pendências restantes

1. Commit docs `ef659e5` (pós-execução FIX-ELEVENORCA-001) só em `fix/orca-browser` — abrir PR → `main`.
2. **PAIR-ORCA-001 em andamento**: subir `orca serve`, capturar Pairing URL, colar em produção.
3. iOS IPA: baixar artifact do Actions (workflow "iOS Build") e instalar via Sideloadly (7 dias revalidar).
4. Validação em aparelho real: conectar device Android/PC e testar agente end-to-end.

## Próximo Passo

- Executar `orca serve --port 6768 --pairing-address <host> --json` e capturar `pairing.url` do stdout.
