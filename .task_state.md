# Task State — v2.12.2-alpha (Pendências executadas)

## Objetivo

Executar todas as pendências listadas (release APK, re-trigger pós-aprovação, fixes desktop/infra) e gerar relatório.

## Concluído

- APK release assinado (debug keystore) 6.4MB, versão 2.12.1/19
- Re-trigger pós-aprovação: agent-bus + approveJob emite `agent:job-resume` + ChatPanel continua a conversa
- PC Agent: CORS default ampliado; settings `timezone`/`region`; Tauri spawn com JWT_SECRET default
- Servidor Node movido para `dist-server/` (vite não apaga mais server.js)
- Instaladores desktop 2.12.1 + /downloads atualizados (setup.exe, MSI, APK release)
- Bump 2.12.2-alpha + relatório `relatorios_agente/TASK-PENDENCIAS_20260921_relatorio.md`
- lint 0 erros · testes web 130/130, ia 257/257 · builds OK · commit 1e06ab5 pushado

## Em Andamento

- Nenhum

## Próximo Passo (bloqueados por ambiente)

- Aplicar migração Supabase: `supabase link --project-ref uacqekmejviqddrbnfhg` + `supabase db push`
- Deploy da branch (PR no GitHub)
- Build iOS (requer macOS)
- Envs `NEXT_PUBLIC_DOWNLOAD_*_URL` (opcional, há fallback)
