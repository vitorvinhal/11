# Task State — v2.11.0-alpha (Agente de Dispositivo PC + Mobile)

## Objetivo Global

Agente PC (desktop) e Agente Mobile como agente 100% funcional do dispositivo — arquivos, fotos, apps, configurações, tela e sistema — visível apenas nos apps nativos (base: nousresearch/hermes-agent).

## Concluído

- Contrato `@11/shared` (device.ts) + migração `devices`/`device_jobs` + RLS
- Pareamento real: `/api/devices/register` (deviceId persistente + secret único)
- Fila de jobs + polling HTTP (Vercel-compatível): jobs/poll/result/approve
- Function calling ligado no 9Router (tools + tool_calls; JSON e SSE) — 3 testes novos
- `agent-core`: 21 tools `device.*`, roteamento nuvem↔device, aprovação `awaiting_approval`
- PC: `device-tools.ts` (21 tools Windows) + `/device/pair` + `/device/tool` + spawn no boot do Tauri; fix tsc pré-existente (dist gerado)
- Mobile: 8 plugins Capacitor + `DeviceBridge` injetado no WebView
- UI: gating nativo puro (agent→desktop-app, mobile→mobile-app), painéis reescritos, Chat usa `/api/agent`+deviceId
- Bump 2.11.0-alpha + CHANGELOG + version.json; lint 0 erros; testes + build root OK; smoke Windows OK

## Em Andamento

- Nenhum — entrega concluída (commit pendente)

## Próximo Passo

- Commit + push; aplicar migração Supabase; build nativo mobile (cap sync) para validar bridge
