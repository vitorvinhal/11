# Task State — v2.12.2-alpha (DEPLOY PRODUÇÃO OK)

## Objetivo Global

Agente de Dispositivo (PC/Mobile) + sistema de atualização + pendências + deploy em produção com downloads novos.

## Concluído

- Deploy de produção via CLI ✅ (rootDirectory do projeto corrigido p/ apps/web)
- Produção LIVE (`candlefish.vercel.app` / `11-app-sage.vercel.app`):
  - version 2.12.2-alpha code 20
  - /downloads/11-desktop-setup.exe → **1522308 bytes** (2.12.2)
  - /downloads/11-mobile.apk → **6760070 bytes** (release 2.12.2)
  - /api/updates ok
- iOS preparado (permissões câmera/fotos/microfone no capacitor.config) — build requer macOS
- Merge na main: `099599e`→...→ `8a7a52c` (+ revert buildCommand)
- Fix ambiente: rootDirectory do projeto Vercel estava vazio → setado `apps/web`

## Pendências restantes (exigem ambiente/credenciais)

1. **Migração Supabase** — senha do DB não encontrada em nenhum lugar acessível (.env/.env.local = placeholders vazios). Rodar:
   ```
   supabase link --project-ref uacqekmejviqddrbnfhg
   supabase db push
   ```
2. **iOS** — build e public no App Store exigem macOS/Xcode.

## Próximo Passo

- Nenhum bloco de código pendente. Validar agente de dispositivo end-to-end com um device real.
