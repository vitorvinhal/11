# Task State — v2.10.16-alpha (Eleven Code = bundle único do Orca)

## Objetivo Global

Substituir o sistema antigo "Eleven Coder" (Monaco/XTerm) pela UI completa do **Orca** (stablyai/orca, MIT) embutida como bundle único na aba **Eleven Code** (desktop-app, desktop-web, mobile-app), sem iframe e sem servidor próprio.

## Concluído

- Bundle Orca disponível em `apps/web/public/orca-inline/` (entry ESM `orca.js` exporta `mountOrca`)
- `ElevenOrca.tsx`: carrega CSS + bundle via `import(/* webpackIgnore */ url)` e monta em `<div>`; fallback de erro
- Código legacy removido: `CodePanel`, `CodeWorkspace`, `ElevenCoder`, `TerminalPane`, `TerminalPanel`, `monaco-setup`, `monaco-languages/`, rota `/coder`, `build-monaco-workers.mjs`
- Deps removidas do web: `@monaco-editor/react`, `monaco-editor`, `@xterm/*`
- `Sidebar.tsx`: aba "coder" removida; "Eleven Code" mantida nas 3 plataformas
- `vercel.json`, `layout.tsx`, `.eslintrc.cjs`, `SessionsPanel.tsx` ajustados
- Validações: lint 0 erros · 16 suites / 130 testes pass · build web OK · shared/ia OK
- Bump `pnpm version:patch` → **2.10.16-alpha**; CHANGELOG atualizado

## Em Andamento

- (nenhum — conclusão)

## Próximo Passo

- Commit (bump de versão + changelog + alterações) e push → validar aba Eleven Code com Orca WebConnect em produção
