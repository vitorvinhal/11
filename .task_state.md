# Task State — v2.10.5-alpha (Fix UI + Testes completos)

## Objetivo Global

Corrigir 5 bugs reais encontrados no teste completo da interface (2026-09-19) e garantir regressão verde.

## Concluído

- **B2 Plugins**: `/api/plugins` usava coluna `config` inexistente → trocado para `metadata` (GET/POST/DELETE 200, persistência confirmada, DB limpo)
- **B1 Health dashboard**: `/health` e `/admin` iteravam `checks[]` → API retorna `services{}`; páginas corrigidas (ALL OK, tabela de services)
- **B5/D1 Admin**: fetches do `/admin` sem Bearer (401) → agora usam `getAccessToken`; parse flat de `/api/version`; metrics shape real (`riskDistribution`); e2e smoke + docs/api.md atualizados (e2e 20/20)
- **B4 PC Agent**: `callPCAgent` enviava body em GET → sem body; GET health reflete `degraded`
- **B3 CSP**: `script-src` allowlist `cdn.tailwindcss.com`, `unpkg.com`, `va.vercel-scripts.com` → Canvas preview estilizado (gradiente), sem bloqueio CSP
- Regressão: lint 0 erros, jest 385/385, `pnpm build` OK, e2e Playwright 20/20, probe UI pós-fix (`/health`,`/admin`,`/canvas`) sem pageerror
- RELATORIO_TESTE.md criado (auditoria completa) + screenshots em `apps/web/_shots/`

## Em Andamento

- Import de skill libraries externas (mattpocock-skills + OpenMontage) em `skills/`

## Próximo Passo

1. Commit do import + push para `main` (inclui fix `4eee77b` pendente)
