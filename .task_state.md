# Task State — v2.12.2-alpha (Eleven Code = Orca + deploy em produção)

## Objetivo Global

Agente de Dispositivo (PC/Mobile) + sistema de atualizações + pendências + deploy em produção + **aba Eleven Code substituída pelo cliente web do Orca (bundle único, regenerado no deploy)**.

## Concluído

- Agente PC/Mobile funcional (21 tools device.*, pareamento, jobs, approval)
- Sistema de upgrade: notificação por versão, aba Atualizações no perfil, downloads
- Pendências: APK release assinado, re-trigger pós-aprovação, fixes desktop (dist-server, CORS, JWT default)
- **Deploy em produção**: mergeado via PR #2 (main) + fix buildCommand cwd-independente — live em `https://11-app-sage.vercel.app`
- `/api/updates` e `/downloads/*` respondendo 200 em produção
- **Eleven Code = Orca**: `ElevenOrca.tsx` monta bundle (`mountOrca`) sem iframe; legacy Eleven Coder/Monaco/XTerm removidos; `scripts/build-orca-bundle.mjs` regenera o bundle no deploy (clone pinned `e476193b` → `vite.eleven.config.ts`) com fallback resiliente (status 0); `vercel.json` roda o script antes do `next build`
- lint 0 erros · testes web 130/130 · build OK · CI gate green (node 24 + webhooks `node:crypto`)

## Em Andamento

- Merge do PR #1 (feat/eleven-code-orca) na main após análise de conflitos (merge local concluído)

## Única pendência bloqueada por ambiente

1. **Migração Supabase** (`20260919_devices_gateway.sql`): sem credenciais (token/db password) neste ambiente.
   Comando (rodar onde houver acesso):
   ```bash
   supabase link --project-ref uacqekmejviqddrbnfhg
   supabase db push
   ```
2. **iOS**: requer build em macOS (este ambiente é Windows).
