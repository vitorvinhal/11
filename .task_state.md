# Task State — v2.10.0-alpha (Deploy Fixed + Bug Fixes)

## Objetivo Global

Fix all 16 broken features from screenshots, deploy to Vercel successfully.

## Concluído

- **Vercel build fixed**: corepack + pnpm@8.15.9 install, correct buildCommand
- **Chat "#" fix**: response validation (min 2 chars, no special-char-only), fallback cascade to other providers
- **Provider selector**: dropdown in ChatPanel with Arcenal, Claude Sonnet 4.5, GLM-5, Gemini, Ollama
- **parse-completion.ts**: SSE detection by line format (`data: {`) not substring `data:`
- **Neural Graph**: random init near cluster centers, stronger repulsion (0.08), cluster attraction (0.006)
- **FinOps**: fixed `created_at` → `invoked_at`, UUID session_id via toUuid()
- **Profile avatar**: Sidebar shows `user.user_metadata.avatar_url` when available
- **cost-breaker.ts**: UUID generation without Node crypto (Math-based)
- **Canvas preview**: CDN error indicator + fallback message when scripts fail to load
- **Agent PC**: platform filter changed to `desktop-web` + WebSocket port fixed to 3001
- **Skills Discover**: `/api/skills/discover` searches GitHub repos + UI shows external results with source badges
- **Plugins Discover**: `/api/plugins/discover` searches GitHub repos + UI shows external results with loading state
- **AGENTS.md**: Regra obrigatória de versionamento + changelog adicionada
- Git: commits bc80d32, b29ef52, 847cab0 pushed to main
- Vercel Production: READY at https://11-i8avkdi2q-vitorvinhal90-8297s-projects.vercel.app

## Em Andamento

- Version bump + CHANGELOG update (pre-commit requirement)
- Commit all fixes

## Próximo Passo

1. Run `pnpm version:patch` to bump version
2. Update CHANGELOG.md with all fixes
3. Commit and push all changes
4. Verify all fixes work on live production URL
