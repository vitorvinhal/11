# Task State — v2.16.3-alpha (integração multi-agente concluída)

## Objetivo Global

INTEGR-AGENTS-001: integrar branches dos agentes (whiting, fix/orca-browser, main) em `integration/agents-20260923` + padronizar segurança (`requireUser`, SSRF allowlist, path.relative).

## Concluído

### TASK-UNIFY-AUTH-001 (base candlefish)

- `apps/web/src/lib/auth-unify.ts` criado (`requireUser(req)` → `{userId,email}`)
- `terminal/exec/route.ts` + `code/route.ts` + `code/services/security.ts` migrados
- `console.log`→`console.warn` em `packages/ia`; linha `coder` removida do CLAUDE.md
- lint 0 erros · 415 testes pass · builds shared/ia/web ok

### TASK-OLLAMA-LLAMA (branch whiting — mergeado)

- Fix default Ollama `llama3.2:3b`, fallback de modelo inexistente via `/api/tags`
- `OLLAMA_POPULAR` ampliado com Llama
- Merge `vitorvinhal/whiting` → commit `f647acf`

### Fix Eleven Code (FIX-ELEVENORCA-001) — CONCLUÍDO

- [x] Shim `process`/`Buffer`, CSS orca, build-orca-bundle
- [x] Merge `fix/orca-browser` → commit `9ffa295`

### INTEGR-AGENTS-001 (Agent-4) — CONCLUÍDO

- [x] Merge `main` → `c65737f` (+ `fix/orca-agent1` → `b3cc100`)
- [x] Security post-merge → `7529e70`:
  - `verifyToken` = 0 em `apps/web/src`
  - `requireUser` em `code/read`, `health/all`, `plugins/discover`, `skills/discover`, `pc-agent` GET, `mobile-agent` GET
  - `resolveCwd` valida com `isUnderRoot` (`path.relative`, não `startsWith`)
  - Sidebar tab code com `mobile-app`; `pnpm-workspace.yaml` = `apps/*` + `packages/*`
  - CHANGELOG combinado sem bump (Brain cuida do release)
- [x] Gates: lint 0 erros · build OK · test 157 passed (web 16/16)
- [x] Relatório: `relatorios_agente/INTEGR-AGENTS-001_20260923_relatorio.md` com BRAIN_SYNC

### Pareamento Orca (PAIR-ORCA-001) — EM ANDAMENTO

- [x] Comando `orca serve --pairing-address <host> --json`
- [ ] Executar `orca serve` local e capturar Pairing URL
- [ ] Colar URL na UI e validar conexão

## Pendências restantes

1. **Smoke tests**: orca 4001 + pc-agent 3001 (REQUIRES_SMOKE_TEST: YES)
2. **Push** da branch `integration/agents-20260923` (nunca main) + PR
3. **iOS IPA**: artifact do Actions → Sideloadly (7 dias revalidar)
4. **Validação em aparelho real**: device Android/PC end-to-end
5. **PAIR-ORCA-001**: subir `orca serve`, capturar Pairing URL

## Próximo Passo

- Push `integration/agents-20260923` → smoke tests 4001/3001 → Brain revisa version bump/CHANGELOG.
