// {{spec.name}} — gerado por @11/cli
export interface {{spec.identifier}}Spec {
  version: string;
  description: string;
}

export const spec: {{spec.identifier}}Spec = {
  version: "{{spec.version}}",
  description: "{{spec.description}}",
};